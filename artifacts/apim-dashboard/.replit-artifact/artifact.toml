kind = "data-app"
previewPath = "/"
title = "API Gateway Metrics Dashboard"
version = "1.0.0"
id = "artifacts/apim-dashboard"
router = "path"

[[integratedSkills]]
name = "data-visualization"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24301

[services.development]
run = "pnpm --filter @workspace/apim-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/apim-dashboard", "run", "build" ]
publicDir = "artifacts/apim-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24301"
BASE_PATH = "/"
